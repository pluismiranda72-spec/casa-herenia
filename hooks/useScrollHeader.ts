"use client";

/**
 * Hook para comportamiento del header (ej. scale en scroll).
 * El BrandHeader usa Framer Motion useScroll; este hook queda para lógica reutilizable si se necesita.
 */
export function useScrollHeader() {
  return {};
}
